/* global describe, it */
/* eslint-disable no-unused-expressions */
import {expect} from 'chai';
// import gulp from 'gulp';
import polyfills from '../dist/gulp-polyfill';

describe('gulp polyfills', () => {
    it('should pass', () => {
        expect(true).to.be.true;
    });
});
